apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-monitor
  namespace: ashapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask-monitor
  template:
    metadata:
      labels:
        app: flask-monitor
    spec:
      serviceAccountName: monitor-sa
      containers:
      - name: flask-monitor
        image: ashwanth01/flask-monitor:be82da4c7e824e4131ba12a3ae098c4717372010
        ports:
        - containerPort: 6000
        env:
        - name: MYSQL_URI
          value: "mysql+pymysql://root:rootpass@mysql.ashapp.svc.cluster.local:3306/ashdb"
        # --------------------------
        # Resource Requests & Limits
        # --------------------------
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"


